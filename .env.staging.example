# Staging (Neon staging branch). Copy to .env.staging and set values. Do not commit .env.staging.
DATABASE_URL=
NODE_ENV=development
PORT=5001
APP_ENV=staging

# Optional
# SEED_ON_START=false
# ADMIN_ALLOWLIST_PHONES=9999977777
